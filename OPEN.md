# OPEN — Auris Play

Открытые вопросы. Пункт = одна строка: вопрос, чем блокирует, кто решает.
Закрытые снимаются отсюда и уезжают в `DECISIONS.md`.

Обновлено: 2026-09-03

1. **Резать ли седьмую коллекцию?** — при 5-6 один-два прокола уха не заполняются
   никогда, семь делает объект цельным; блокирует финальный вид `drawEarDots` и
   объём арт-работы; решает Алёна.
2. **`PIECES_PER_COLLECTION = 3` — подтвердить измерением.** — из ~5 с на готовую
   фишку выходит ступень ~15 с и восхождение ~1:45, но это арифметика; блокирует
   тюнинг темпа; решает `smoke.html`/`tuning.html` после правки правил.
3. **Какие 2-3 новые коллекции и из каких съёмок.** — в `PROMO_Auris/PhotoShoots/`
   21 съёмка, больше всего свежего материала в `2023` (80 кадров) и `2024` (31);
   блокирует всю работу по спрайтам; решает Алёна.
4. **Перекраивать ли Marchesa.** — источник 469×444, уже помечен как мягкий, при
   росте числа коллекций разница станет заметнее; блокирует ничего, но дешевле
   сделать одним заходом с новыми кропами; решает Алёна.
5. **Согласие Влада на коммерческое использование.** — личный слой снят, но
   фотография, логотипы и марка Auris остаются, и `LICENSE` §3 прямо требует его
   согласия для промо-версии; блокирует публикацию под именем Auris, не разработку;
   решает Алёна с Владом.
6. **Куда указывают QR-коды.** — `aurisjewellery.com` и `scalpelburg.com` целиком
   или конкретные разделы; блокирует финальную вёрстку экрана коллекций; решает
   Алёна.
7. **Где хостится и под каким адресом.** — свой репозиторий на Pages решён [Р-008],
   но имя репозитория, домен и нужен ли CNAME на поддомен Auris не выбраны;
   блокирует бейдж в `README` и настройку Pages; решает Алёна.
