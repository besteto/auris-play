# STATE — Auris Play (студийная игра)

Снимок настоящего. Читается целиком на входе в блок (/brief), переписывается на
выходе (/park). История живёт в `DECISIONS.md`, здесь только то, что нужно знать
сейчас.

Обновлено: 2026-09-15 (блок 6)

## Где стоим

Реализация, ветка `kiosk`. Закрыто пятнадцать заходов из семнадцати плюс пакет
хвостов: игра играется целиком, **вся копия ходит по таблице RU/EN**, переключатель
языка живой, выбор языка переживает перезагрузку. Хостинг решён [Р-031]: свой
репозиторий Алёны на GitHub Pages, ветка `kiosk`. Remote в репозитории ещё не заведён —
это первое действие следующего блока, и оно в руках Алёны.

## Активный заход

- ID: план `docs/superpowers/plans/2026-09-03-auris-kiosk-implementation.md`
- Спека: `docs/superpowers/specs/2026-09-03-auris-kiosk-design.md` (ревизия 3)
- Статус: в сборке, 15/17; на приёмке — первый живой показ с телефона
- Периметр: весь `auris-play`, ветка `kiosk`. `main` держит базу и канон.
- Журнал исполнения: `.superpowers/sdd/2026-09-03-auris-kiosk-implementation/progress.md`
  плюс `task-10-report.md` и `task-tails-report.md`.

## Осталось сделать

| Заход | Что | Чем проверяется |
|---|---|---|
| 12 | QR офлайном, две ветки экрана контактов, service worker | Pages по https, телефон |
| 14 | Права: `LICENSE` §2/§3, **README** (ещё подарочный, с `ref-35`), остатки личного слоя | грепом |
| 11 | `Kiosk.is()` по `#kiosk`; сброс по простою и аттракт-луп **только под ним** | `pagecheck.sh` + браузер |
| 16 | `DEVELOPING.md`: формат поставки набора, рунбук киоска | чтение |
| 17 | Микротипографика на `clamp()`, поднос не трогать | 412×915 глазами |

Порядок изменён против плана: 12 и 14 раньше 11, потому что ветка теперь публична —
README и контакты видны первыми, а киоска ещё нет. У 11, 12, 14 в начале стоят врезки
«поправки ревизии 3» — читать до шагов.

Финального ревью всей ветки ещё не было — оно после 16.

## Действующие законы

- `ref-35` заморожен: один коммит, доставленный подарок [Р-001]. Скриншоты с
  `besteto.github.io/ref-35/` — не эта ветка.
- **Носитель определяется флагом, никогда не догадкой** [Р-028]. По умолчанию телефон,
  киоск — по `#kiosk`. Сброс по простою и аттракт-луп — только на киоске; сегодня их
  нет в коде вовсе.
- QR рисуется только на чужом экране, один за раз, `min(72vw, 60vh, 420px)` [Р-029].
- `src/rules.js` и `src/manifest.js` **чистые**: без DOM, без импортов.
- **`START_PIECES = 10`, `SPAWN_BIAS = 0.65`, поднос `min(94vw, 60vh, 440px)` не
  трогать** [Р-014, Р-021, Р-030].
- На подносе всегда ровно три сета, пока заход идёт [Р-014].
- `relieve()` безусловен, в нормальной игре недостижим [Р-026].
- Конец демо — плита коллекции в тот же тик, что и последний ретайр [Р-032].
- `state.done` — множество различных ключей, не журнал [Р-025].
- `src/game.js` — единственный файл, трогающий DOM.
- Всё называемое приходит из манифеста в рантайме [Р-010, Р-023]; статичная копия —
  из `data/copy.js`, по `data-i18n`, `data-i18n-aria`, `data-i18n-alt`.
- По-английски «витрина» — `showcase`; `tray` — только поднос, игровая сетка.
- `PROMO_Auris/` и `choosed/` не коммитятся никогда.
- Никакого личного слоя под именем Auris [Р-007].
- Игра ничего не измеряет о человеке.

## Последние решения

- Р-027 — гейт из терминала: `tools/harness.sh`, `tools/pagecheck.sh`
- Р-028 — носитель по флагу: телефон по умолчанию, киоск по `#kiosk`
- Р-029 — QR только на чужом экране, один за раз, во всю плиту
- Р-030 — микротипографика на `clamp()`, игра не трогается
- Р-031 — хостинг: свой репозиторий на GitHub Pages, ветка `kiosk`
- Р-032 — **демо заканчивается на плите коллекции немедленно**, без плиты последнего сета

## На приёмке

**Проверено на ПК** (блок 6): конец демо переделан по итогам. **Первый показ с телефона.** Завести remote, запушить `kiosk`, включить Pages с этой ветки,
открыть `https://<user>.github.io/<repo>/` на телефоне. Смотреть: переключатель
RU / EN на титуле, английские тексты на всех экранах (инструкция, тосты, доска,
контакты, плиты), надпись `Studio Edition` одной строкой, крупность подноса и
микротипографики (заход 17 стоит на этом), звук. Экран контактов покажет ветку
общего экрана — телефонной ветки нет до захода 12.

Английские подписи кнопок без текста (`Menu`, `Sound`, `Close`, `Scalpelburg studio`)
сочинил сборщик — проверить регистр.

README на GitHub ещё подарочный: «Birthday Edition», бейдж на `ref-35`. Заход 14.

## Следующий шаг

Алёна заводит репозиторий и Pages, смотрит с телефона; следующий блок открывается
заходом 12 (контакты и QR по https) или правками по итогам показа.

## Состояние репозитория

- Ветка `kiosk`, 23 коммита над `main` (5). История переписана 2026-09-15 под формат
  `CONTRIBUTING.md`, фиксапы засквошены; старая история — теги `backup/*-2026-09-15`,
  удалить после пуша. Remote не задан.
- `CONTRIBUTING.md` — для людей, `AGENTS.md` — для агентов; оба читать до правок.
- Харнесс: `tests.html` 152/152, `smoke.html` 5/5, `tuning.html` 6/6.
- `bash tools/pagecheck.sh '#endless' '#demo/cassiopea' '#collections' '#board' '#contacts'`.
- Прогон всего: `bash tools/harness.sh tests smoke tuning`.
- Локально для телефона: `python -m http.server 8732 --bind 0.0.0.0`.

## Не проверено

- **Ничего из ветки не открывалось на телефоне.** RU/EN проверен headless-скриптом по
  DOM (0 кириллицы в 42 узлах и в атрибутах после `applyCopy('en')`), не глазами.
- Драг мышью и пальцем: игра гонялась программно через `window.Game`.
- Звук ни разу не слушали.

## Открытые вопросы

8 штук, полный список в `OPEN.md`. Топ-3:

1. **Как называется доска на личном телефоне** — «Рекорды дня» стоит с `TODO(open-8)`.
2. **Как человек попадает на игру с телефона** — определяет объём захода 12.
3. **Перекроить Марчезу** — до новой съёмки не может быть выделенной в демо.
